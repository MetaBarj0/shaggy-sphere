{
  "name": "shaggy-sphere",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev --port ${FRONTEND_EXPOSED_PORT} --host",
    "build": "astro build",
    "preview": "astro preview --port ${FRONTEND_EXPOSED_PORT} --host",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/preact": "^5.0.1",
    "@astrojs/rss": "^4.0.17",
    "astro": "^6.0.5",
    "preact": "^10.29.0"
  }
}
